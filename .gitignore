# ── Build artifacts (NDK cross-compilation) ──────────────────────────
build_android/
deps/
*.o
*.a
*.so
.cxx/

# ── OSRM binary data (generated, too large for git) ─────────────────
osrm_data_v5271/
*.osm.pbf
*.osrm
*.osrm.*

# ── External symlink: myosm → ../myosm ──────────────────────────────
# Setup: ln -s ../myosm myosm  (see README.md)
myosm

# ── Bundled OSRM binary (built from source; copy manually) ──────────
# Build: ./scripts/build_osrm_android.sh && cp build_android/install/bin/osrm-routed android/app/src/main/assets/osrm-routed
android/app/src/main/assets/osrm-routed

# ── Android Studio / Gradle ─────────────────────────────────────────
android/.gradle/
android/build/
android/app/build/
android/local.properties
*.iml
*.apk
*.aab
*.dex
*.class
*.ap_

# ── IDE / editor ────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── System ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.bak

# ── Logs & reports ──────────────────────────────────────────────────
*.log
reports/

# ── Misc temp ───────────────────────────────────────────────────────
*.tmp
*.cache
